version: "3.9"

services:
  python3:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5001:80"

    volumes:
      - .:/app

    environment:
      FLASK_APP: 'apps.app:create_app("local")'
      FLASK_ENV: development
